{
  "name": "pascal-playground",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Interactive browser playground for pascal-toolchain: paste Pascal and watch tokens, AST, formatted source and compiled JS update live.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vitest": "^2.1.4"
  }
}
